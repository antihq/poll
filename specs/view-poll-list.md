# Done

- `/polls/`
- Show a table of all polls for the team organization
- The table contains the poll name and the number of responses
- Poll list displays current active polls (deleted polls are hidden but still count against quota)
- Team poll usage is tracked cumulatively for limit enforcement
