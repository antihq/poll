- `/polls/{poll}`
- Users can view a poll and see the number of responses received
- List all poll answers, with each answer showing a percentage of responses.

- `/polls/{poll}/answers/{answer}`
- To see answer details
- Display a list of responses with the date and contact email if available
