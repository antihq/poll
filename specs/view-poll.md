# Done

- `/polls/{poll}`
- Users can view a poll and see number of responses received
- List all poll answers, with each answer showing a percentage of responses

- `/polls/{poll}/answers/{answer}`
- To see answer details
- Display a list of responses with date, contact email and feedback if available

- Users can delete poll
- Access requires active subscription OR fewer than 1000 polls created by team (cumulative count)

- `/polls/{poll}/answers/{answer}`
- To see answer details
- Display a list of responses with date, contact email and feedback if available
- Access requires active subscription OR fewer than 1000 polls created by team (cumulative count)

- Users can delete poll
- Access requires active subscription OR fewer than 1000 polls created by team (cumulative count)
- Deleting polls does not free up quota slots due to cumulative counting system

- Users can delete the poll
- Access requires active subscription OR fewer than 1000 polls created by team
